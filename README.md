The dumbest simplest way I imagined to download IIIF images from a manifest; written in 10 minutes. Intended to be improved later.

Only dependency is `requests`. Simplest way to run is to install uv and then `uv run download_iiif.py`
